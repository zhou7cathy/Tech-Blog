<!-- Loop over each post in the posts array -->
{{#each posts as |post| }}
<div class="row mb-8 post">
  <div class="col-md-5">
    <h2>
      <a href="/post/{{post.id}}">{{post.title}}</a>
    </h2>
    <p>
      Post on {{format_date post.create_date}}
    </p>
  </div>
  <div>
    <p>
      {{post.contents}}
    </p>
  </div>
</div>
{{/each}}